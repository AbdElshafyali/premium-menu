# 🚀 دليل الإعداد السريع

## الخطوة 1: إعداد Supabase

### 1.1 إنشاء مشروع جديد
1. اذهب إلى [supabase.com](https://supabase.com)
2. اضغط "Start your project" أو "New Project"
3. أنشئ حساب جديد (أو سجل دخول)
4. اختر:
   - اسم المشروع: `menu-system`
   - كلمة مرور قاعدة البيانات: (احفظها!)
   - المنطقة: اختر الأقرب لك

### 1.2 إنشاء الجداول
1. في لوحة تحكم Supabase، اذهب لـ **SQL Editor**
2. اضغط "New Query"
3. انسخ محتوى ملف `database.sql` بالكامل
4. الصق في المحرر
5. اضغط **Run** (أو Ctrl+Enter)
6. انتظر حتى تظهر "Success"

### 1.3 إعداد Storage للصور
1. اذهب لـ **Storage**
2. اضغط "Create a new bucket"
3. الاسم: `restaurant-images`
4. ✅ فعّل "Public bucket"
5. اضغط "Create bucket"

#### إعداد Policies للصور:
1. اضغط على `restaurant-images` bucket
2. اذهب لـ **Policies**
3. اضغط "New Policy"
4. اختر "For full customization"
5. انسخ والصق كل policy من التالي:

**Policy 1: السماح برفع الصور**
```sql
CREATE POLICY "Allow authenticated users to upload"
ON storage.objects FOR INSERT
WITH CHECK (
  bucket_id = 'restaurant-images' 
  AND auth.role() = 'authenticated'
);
```

**Policy 2: السماح بقراءة الصور (الجميع)**
```sql
CREATE POLICY "Allow public read access"
ON storage.objects FOR SELECT
USING (bucket_id = 'restaurant-images');
```

**Policy 3: السماح بحذف الصور (المالك فقط)**
```sql
CREATE POLICY "Allow users to delete own images"
ON storage.objects FOR DELETE
USING (
  bucket_id = 'restaurant-images' 
  AND auth.uid() = owner
);
```

### 1.4 نسخ المفاتيح
1. اذهب لـ **Settings** → **API**
2. انسخ:
   - **Project URL** (مثلاً: `https://xxxxx.supabase.co`)
   - **anon/public key** (المفتاح الطويل)

---

## الخطوة 2: تعديل الإعدادات في المشروع

### 2.1 افتح `config/supabase.js`
```javascript
const SUPABASE_URL = 'https://xxxxx.supabase.co'; // ضع الـ URL هنا
const SUPABASE_ANON_KEY = 'eyJh...'; // ضع المفتاح هنا
```

---

## الخطوة 3: إنشاء أول مستخدم (مدير)

### 3.1 إنشاء حساب Admin
1. في Supabase Dashboard → **Authentication** → **Users**
2. اضغط "Add user"
3. اختر "Create new user"
4. املأ:
   - Email: `admin@example.com` (استخدم إيميلك)
   - Password: `password123` (كلمة مرور قوية)
   - ✅ Auto Confirm User
5. اضغط "Create user"

### 3.2 إضافة بيانات المطعم
1. اذهب لـ **SQL Editor**
2. شغّل الكود ده (غيّر البيانات حسب مطعمك):

```sql
INSERT INTO restaurants (admin_email, name, name_en, default_language)
VALUES (
  'admin@example.com',  -- نفس الإيميل اللي عملته
  'مطعم الذواقة',
  'Gourmand Restaurant',
  'ar'
);
```

---

## الخطوة 4: تشغيل المشروع

### طريقة 1: فتح مباشر (أسهل)
1. افتح `admin/login.html` في المتصفح
2. سجل دخول بالإيميل والباسورد اللي عملتهم
3. ابدأ إضافة أقسام ومنتجات! 🎉

### طريقة 2: Local Server (أفضل)
```bash
# إذا عندك Python
cd basic-menu
python -m http.server 8000

# أو Node.js
npx http-server -p 8000

# بعدين افتح:
# http://localhost:8000/admin/login.html
```

---

## الخطوة 5: إضافة محتوى

### 5.1 إضافة أقسام
1. بعد تسجيل الدخول، اضغط **➕ إضافة قسم**
2. املأ:
   - الاسم (عربي): "المشروبات"
   - الاسم (إنجليزي): "Beverages"
   - الأيقونة: 🥤
3. احفظ

### 5.2 إضافة منتجات
1. اضغط **➕ إضافة منتج**
2. املأ البيانات:
   - الصورة
   - الاسم (عربي + إنجليزي)
   - الوصف
   - السعر
   - القسم
3. احفظ

### 5.3 مشاركة المنيو
1. اذهب لقسم "مشاركة المنيو"
2. انسخ الرابط أو حمّل QR Code
3. شارك مع العملاء! 🎊

---

## 🎯 الخلاصة

الآن عندك منيو إلكتروني جاهز! 🎉

**الوقت المتوقع:** 15-20 دقيقة للإعداد الكامل

**التكلفة:** 0 جنيه - كل شيء مجاني! ✅
